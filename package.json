{
  "name": "lifescope-xr",
  "version": "1.0.0",
  "description": "lifescope extended reality",
  "author": "(https://lifescope.io)",
  "scripts": {
    "dev": "nuxt",
    "debug": "nuxt build && nuxt start --inspect-brk=3333",
    "build": "nuxt build",
    "start": "nuxt build && nuxt start",
    "generate": "nuxt generate",
    "lint": "eslint --ext .js,.vue --ignore-path .gitignore .",
    "precommit": "npm run lint"
  },
  "dependencies": {
    "aframe": "^0.8.2",
    "aframe-geojson-component": "^1.2.4",
    "aframe-layout-component": "^4.3.1",
    "isomorphic-fetch": "^2.2.1",
    "nuxt": "^1.4.0"
  }
}
